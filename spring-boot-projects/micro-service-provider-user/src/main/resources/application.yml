server:
  port: 80
spring:
  datasource:
    driver-class-name: com.mysql.jdbc.Driver
    url: jdbc:mysql://localhost:3306/test?useUnicode=true&characterEncoding=utf8&serverTimezone=UTC
    username: root
    password: root

  jpa:
    hibernate:
      ddl-auto: none
    generate-ddl: false
    show-sql: true
  application:
    name: "micro-service-provider-user-application"
eureka:
  client:
    register-with-eureka: false
    fetch-registry: false
